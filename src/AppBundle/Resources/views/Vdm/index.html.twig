{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vdm list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Content</th>
                <th>When</th>
                <th>Author</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('vdm_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.content }}</td>
                <td>{% if entity.when %}{{ entity.when|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.author }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('vdm_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('vdm_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('vdm_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
